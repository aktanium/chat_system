# ===========================
# CHAT SYSTEM .ENV CONFIG
# ===========================

# SERVER
PORT=3000
NODE_ENV=development

# DATABASE (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=chat_system
DB_USER=postgres
DB_PASSWORD=postgres

# REDIS
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# CORS
CORS_ORIGIN=*

# SOCKET.IO
SOCKET_IO_PORT=3000
SOCKET_IO_CORS=*
